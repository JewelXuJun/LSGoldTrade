<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_white"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="@dimen/margin_popupwindow"
            android:layout_marginBottom="@dimen/margin_popupwindow"
            android:textColor="@color/black_deep"
            android:textSize="@dimen/common_font_size" />

        <include layout="@layout/item_line_vertical" />

        <TextView
            android:id="@+id/tv_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/common_area_margin"
            android:lineSpacingExtra="@dimen/lineSpacingExtra_normal"
            android:textColor="@color/color_text_normal"
            android:textSize="@dimen/sp_14" />

        <Button
            android:id="@+id/btn_agree"
            android:layout_width="match_parent"
            android:layout_height="@dimen/height_login_btn"
            android:layout_marginTop="@dimen/margin_bigger"
            android:background="@drawable/bg_btn_blue_solid"
            android:text="@string/transaction_agree"
            android:textColor="@color/white"
            android:textSize="@dimen/sp_16" />

    </LinearLayout>

</layout>
